---
phase: 05.1-certified-randomized-smoothing-baseline-cohen-et-al-2019-l2-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - synth_finetune.py
autonomous: true
requirements: [RS-02]
must_haves:
  truths:
    - "synth_finetune.py --rs_sigma <s> runs a SINGLE-STAGE noise-augmented finetune warm-started from checkpoint/2016.10a_AWN.pkl"
    - "During training AND validation, x has torch.randn_like(x)*sigma added per batch when rs_sigma > 0"
    - "The run saves checkpoint/2016.10a_AWN_rs{sigma}.pkl (one file per sigma)"
    - "Four checkpoints exist: 2016.10a_AWN_rs0.002.pkl, _rs0.005.pkl, _rs0.01.pkl, _rs0.02.pkl"
    - "Each checkpoint loads via torch.load(..., weights_only=True) into AWN without error"
  artifacts:
    - path: "synth_finetune.py"
      provides: "--rs_sigma flag + noise injection in finetune() + rs single-stage branch in main()"
      contains: "rs_sigma"
    - path: "checkpoint/2016.10a_AWN_rs0.01.pkl"
      provides: "Noise-augmented AWN checkpoint at sigma=0.01 (narrative-continuity operating point)"
  key_links:
    - from: "synth_finetune.py:finetune()"
      to: "training batch"
      via: "noise injection gated on noise_sigma > 0"
      pattern: "randn_like\\(sig\\) \\* noise_sigma"
    - from: "synth_finetune.py:main()"
      to: "checkpoint/2016.10a_AWN_rs{sigma}.pkl"
      via: "torch.save save_path"
      pattern: "2016.10a_AWN_rs"
---

<objective>
Add a `--rs_sigma` flag to `synth_finetune.py` implementing Cohen 2019's noise-augmented
training as a SINGLE-STAGE finetune (locked decision #1): warm-start from
`checkpoint/2016.10a_AWN.pkl`, inject `x += torch.randn_like(x) * sigma` per batch in BOTH
the train and val loops, and save `checkpoint/2016.10a_AWN_rs{sigma}.pkl`. Then run the
sweep to produce four checkpoints for the sigma grid {0.002, 0.005, 0.01, 0.02}.

Purpose: RS-02 is a hard prerequisite for RS-03 — certifying the clean checkpoint yields
near-useless radii (Cohen 2019 Sec 3.2). The ONLY new behavior is the noise injection; the
existing early-stopping / LR-schedule / real+synth-mix loop is reused verbatim. This does NOT
compose with `--curriculum` (single-stage only).
Output: modified `synth_finetune.py` + four `2016.10a_AWN_rs*.pkl` checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-certified-randomized-smoothing-baseline-cohen-et-al-2019-l2-/05.1-RESEARCH.md

<interfaces>
<!-- Existing finetune() loop to modify. From synth_finetune.py:234-300 -->
def finetune(model, train_loader, val_loader, device, lr=1e-4, epochs=50,
             patience=8, save_path=None, tag='FT') -> best_val_acc
# train loop: for sig, lab in train_loader: sig,lab = sig.to(device),lab.to(device); logit,regu = model(sig)
# val   loop: for sig, lab in val_loader:   sig,lab = sig.to(device),lab.to(device); logit,regu = model(sig)

<!-- Existing single-stage main() branch. From synth_finetune.py:453-540 -->
# base_ckpt = os.path.join(args.ckpt_path, '2016.10a_AWN.pkl')
# loads with torch.load(ckpt, map_location=device, weights_only=True)
# single-stage: all_sigs = concat(synth_sigs[s_train], real_sigs); make_loaders(...); finetune(...)
# save_path = os.path.join(args.ckpt_path, '2016.10a_AWN_ft.pkl')

<!-- Existing argparse. From synth_finetune.py:370-404 -->
# --channel_preset, --curriculum, --n_per_cell, --ft_epochs, --ft_lr, --ft_patience,
# --batch_size, --seed, --device, --resume, --ckpt_path (default ./checkpoint)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --rs_sigma flag + noise injection to finetune() and a single-stage rs branch in main()</name>
  <files>synth_finetune.py</files>
  <read_first>
    - synth_finetune.py (lines 234-300 finetune loop; 370-404 argparse; 453-540 single-stage main branch)
    - .planning/phases/05.1-certified-randomized-smoothing-baseline-cohen-et-al-2019-l2-/05.1-RESEARCH.md (Architecture Pattern 3 — noise-augmented finetuning; Open Q1 resolution = single-stage)
  </read_first>
  <action>
1. Add `noise_sigma: float = 0.0` parameter to `finetune(...)` signature.
   - In the TRAIN loop, immediately after `sig, lab = sig.to(device), lab.to(device)`, add:
     `if noise_sigma > 0: sig = sig + torch.randn_like(sig) * noise_sigma`
   - In the VAL loop, apply the SAME injection after `sig, lab = sig.to(device), lab.to(device)`
     (Cohen validates under noise — certified accuracy is measured under noise, not clean).
   - Leave early stopping, LR schedule, checkpoint save unchanged.

2. Add argparse flag in `main()`:
   `parser.add_argument('--rs_sigma', type=float, default=0.0,
       help='Cohen-style Gaussian noise-augmented single-stage finetune (raw-IQ units). >0 enables.')`
   Validate: after parse, `if args.rs_sigma < 0: parser.error('--rs_sigma must be >= 0')`.

3. Add a NEW single-stage branch in `main()` gated on `args.rs_sigma > 0`, placed BEFORE the
   existing `if args.curriculum` / single-stage block so it takes precedence and does NOT compose
   with curriculum. In this branch:
     - Warm-start ALWAYS from `2016.10a_AWN.pkl` (base), loaded with
       `torch.load(ckpt, map_location=device, weights_only=True)`. (Ignore --resume/--curriculum here.)
     - Build the same real+synth training mix already used by the single-stage path
       (`all_sigs = concat(synth_sigs[s_train], real_sigs)`; `make_loaders(...)`).
     - `save_path = os.path.join(args.ckpt_path, f'2016.10a_AWN_rs{args.rs_sigma}.pkl')`
       (so sigma=0.01 -> `2016.10a_AWN_rs0.01.pkl`).
     - Call `finetune(model, train_loader, val_loader, device, lr=args.ft_lr, epochs=args.ft_epochs,
       patience=args.ft_patience, save_path=save_path, tag=f'RS{args.rs_sigma}', noise_sigma=args.rs_sigma)`.
     - After training, evaluate on real RML test via the existing `evaluate_on_real(model, device)`
       so the log records clean-test accuracy (sanity: should stay non-trivial).
     - `return` at the end of this branch (do not fall through to curriculum/single-stage).
  </action>
  <verify>
    <automated>python synth_finetune.py --help 2>&1 | grep -q -- --rs_sigma && python -c "import ast; ast.parse(open('synth_finetune.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python synth_finetune.py --help` output contains `--rs_sigma`
    - `grep -n "randn_like(sig) \* noise_sigma" synth_finetune.py` matches (train AND val — 2 hits)
    - `grep -c "randn_like(sig) \* noise_sigma" synth_finetune.py` returns 2
    - `grep -n "2016.10a_AWN_rs" synth_finetune.py` matches
    - `grep -n "weights_only=True" synth_finetune.py` matches in the rs branch load
    - `python -c "import ast; ast.parse(open('synth_finetune.py').read())"` exits 0
  </acceptance_criteria>
  <done>--rs_sigma flag exists; noise injected in train+val; rs branch saves 2016.10a_AWN_rs{sigma}.pkl and is single-stage.</done>
</task>

<task type="auto">
  <name>Task 2: Train the four noise-augmented checkpoints for the sigma grid {0.002, 0.005, 0.01, 0.02}</name>
  <files>synth_finetune.py, checkpoint/2016.10a_AWN_rs0.002.pkl, checkpoint/2016.10a_AWN_rs0.005.pkl, checkpoint/2016.10a_AWN_rs0.01.pkl, checkpoint/2016.10a_AWN_rs0.02.pkl</files>
  <read_first>
    - synth_finetune.py (the --rs_sigma branch added in Task 1)
    - CLAUDE.md (Synthetic Data Generation & Finetuning — typical workflow; `source venv/bin/activate`)
    - .planning/phases/05.1-certified-randomized-smoothing-baseline-cohen-et-al-2019-l2-/05.1-RESEARCH.md (locked sigma grid; GPU verified working RTX 5060 Ti / torch 2.11.0+cu128)
  </read_first>
  <action>
Run the noise-augmented finetune once per sigma in the locked grid {0.002, 0.005, 0.01, 0.02}.
The synthetic dataset only needs to be generated once (reuse `./data/synth_2016.10a.pkl` if present;
otherwise the first run's gen step creates it). GPU auto-detect is sufficient (do not force CPU).

For each sigma s in [0.002, 0.005, 0.01, 0.02] run:
```bash
python synth_finetune.py --mode finetune --rs_sigma <s> \
  --n_per_cell 2000 --ft_epochs 50 --ft_lr 1e-4 --ft_patience 8 --batch_size 256 --seed 42
```
Confirm each run writes `checkpoint/2016.10a_AWN_rs<s>.pkl` and the log shows clean RML test
accuracy staying non-trivial (a collapse to ~random ~9% would indicate sigma too large or a bug —
per research Pitfall 5 / Anti-pattern). If a single run is slow, the four are independent and may be
run sequentially; there is no ordering constraint.
  </action>
  <verify>
    <automated>python -c "import os,torch; from util.utils import create_model; from util.config import Config; c=Config('2016.10a',train=False); c.device='cpu'; m=create_model(c,'awn'); [m.load_state_dict(torch.load(f'checkpoint/2016.10a_AWN_rs{s}.pkl',map_location='cpu',weights_only=True)) for s in ['0.002','0.005','0.01','0.02']]; print('all 4 load OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ls checkpoint/2016.10a_AWN_rs0.002.pkl checkpoint/2016.10a_AWN_rs0.005.pkl checkpoint/2016.10a_AWN_rs0.01.pkl checkpoint/2016.10a_AWN_rs0.02.pkl` lists all four
    - Each checkpoint loads via `torch.load(..., weights_only=True)` into AWN without error (verify command exits 0, prints "all 4 load OK")
    - Training logs show clean RML test accuracy > 30% for each sigma (no collapse to random)
  </acceptance_criteria>
  <done>Four 2016.10a_AWN_rs*.pkl checkpoints exist, load with weights_only=True, and did not collapse to random accuracy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint file → torch.load | Newly-produced rs checkpoints deserialized by RS-03; must use weights_only=True |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-03 | Tampering | rs checkpoint deserialization | mitigate | Warm-start load and all downstream loads use `torch.load(..., weights_only=True)` (project convention) |
| T-05.1-04 | Denial of Service | sigma too large collapses model | accept | Non-security; caught by the >30% clean-acc acceptance check |
</threat_model>

<verification>
- `python synth_finetune.py --help` shows `--rs_sigma`
- Four `checkpoint/2016.10a_AWN_rs*.pkl` exist and load with weights_only=True
- Noise injection present in BOTH train and val loops (grep count == 2)
</verification>

<success_criteria>
A single-stage `--rs_sigma` noise-augmented finetune path exists and has produced four
noise-trained checkpoints (one per locked sigma) that load cleanly and retain non-trivial accuracy.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-certified-randomized-smoothing-baseline-cohen-et-al-2019-l2-/05.1-02-SUMMARY.md`
</output>
