---
phase: 07-benchmark-attack-generation-time-per-sample-cpu-vs-gpu-acros
verified: 2026-04-26T18:10:00Z
status: human_needed
score: 18/18 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visual inspection of paper/latex/figures/attack_bench_latency.pdf"
    expected: "Grouped bar chart with 5 attack groups (FGSM/PGD/CW/EAD-L1/EAD-EN), 2 bars per group (gray=CPU, blue=GPU), log-scale y-axis labelled 'Latency (ms/sample)', visible std error bars, IEEE-style typography matching v1.0 paper figures."
    why_human: "PDF rendering is visual; programmatic checks confirm it is a 1-page valid PDF (13.9 KB) but legend placement, error-bar visibility, axis label rendering, and IEEE styling consistency need an eye."
  - test: "Full-budget regeneration before camera-ready submission"
    expected: "Run `python main.py --mode attack_bench --dataset 2016.10a --ckpt_path ./checkpoint` (defaults: N=512, R=5, batch=128, warmup=3) on the host. Then `cd paper/scripts && python plot_attack_bench_latency.py`. CSV in newest inference dir should show n_samples=512 and n_reps=5; PDF numbers should reflect those values."
    why_human: "Current CSV/PDF reflect a smoke-budget run (n=64, reps=2) — known issue documented in 07-03-SUMMARY.md. Wiring is verified complete; the canonical paper-budget numbers must be regenerated by the user before Phase 6 paper integration. Cannot be performed automatically by the verifier (>5 min wall time, requires GPU)."
---

# Phase 7: Benchmark Attack Generation Time Verification Report

**Phase Goal:** A single command (`python main.py --mode attack_bench --dataset 2016.10a --ckpt_path ./checkpoint`) measures per-sample adversarial generation latency for the 5 paper attacks (fgsm, pgd, cw, eadl1, eaden) on both CPU and GPU; produces a 10-row CSV + env JSON sidecar under `inference/2016.10a_*/result/`; renders an IEEE-styled PDF figure at `paper/latex/figures/attack_bench_latency.pdf`.

**Verified:** 2026-04-26T18:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Single Python call runs both CPU and GPU passes for the 5 paper attacks in one invocation (D-13, D-16) | VERIFIED | `util/attack_bench.py:363` — `for device in ['cpu', 'cuda']:` outer loop with inner ATTACKS_5 iteration; CSV contains exactly the 5x2 = 10 rows |
| 2  | Per-cell warmup of W iterations performed and discarded before any timed iteration (D-02) | VERIFIED | `_time_one_cell` at lines 216-224: warmup loop runs `generate_adversarial` and discards results; timed reps follow at line 228 |
| 3  | GPU timing brackets every timed block with `torch.cuda.synchronize()` before t0/t1; CPU side skips sync (D-03) | VERIFIED | Lines 229-242: `if device == 'cuda': torch.cuda.synchronize()` precedes both `t0 = perf_counter()` and `t1 = perf_counter()`; grep count = 2 |
| 4  | Each (attack, device) cell repeated R=5 times; reported as mean +/- std (D-04) | VERIFIED | `DEFAULT_N_REPS = 5`; `mean_ms_per_sample` and `std_ms_per_sample` computed at line 247-248 via `arr.mean()` and `arr.std(ddof=0)`; CSV has both columns |
| 5  | Sample is N=512 IQ samples stratified across (mod, snr) cells from RML2016.10a test split (D-06) | VERIFIED | `DEFAULT_N_SAMPLES = 512` at line 67; `_stratified_indices` at lines 89-175 builds (label, snr) buckets and round-robin draws |
| 6  | Fixed batch_size=128 throughout (D-07) | VERIFIED | `DEFAULT_BATCH_SIZE = 128`; `_iter_batches` chunks by batch_size; CSV column `batch_size=128` for all 10 rows |
| 7  | Each torchattacks call goes through Model01Wrapper plus minmax/unit IQ adapters (D-15 + project rule) | VERIFIED | Line 60-61: `from util.adv_attack import Model01Wrapper` and `from util.sigguard_eval import create_attack, generate_adversarial`; line 370: `wrapped_model = Model01Wrapper(model)`; `generate_adversarial` handles ta_box internally |
| 8  | Output CSV at `cfg.result_dir/attack_bench.csv` has 10 data rows with required columns (D-08, D-09) | VERIFIED | `inference/2016.10a_165/result/attack_bench.csv` — 12 lines (1 env-comment + 1 header + 10 data); columns match `attack,device,batch_size,n_samples,n_reps,mean_ms_per_sample,std_ms_per_sample,total_seconds` |
| 9  | CSV header includes `# env: ...` comment line and companion `attack_bench_env.json` captures torch/cuda/cpu metadata (D-12) | VERIFIED | CSV line 1: `# env: torch=2.9.0+cu130 cuda=13.0 gpu=NVIDIA GeForce RTX 5060 Ti cpu='x86_64' cores=6`; sibling JSON contains `torch_version`, `cuda_version`, `gpu_name`, `cpu_count`, `platform`, etc. |
| 10 | Function halts early with clear RuntimeError if `torch.cuda.is_available()` is False (D-16) | VERIFIED | `util/attack_bench.py:319-320`: `if not torch.cuda.is_available(): raise RuntimeError("attack_bench requires CUDA. Halting (D-16).")` |
| 11 | Model state_dict reloaded between CPU and GPU passes from snapshot (D-13) | VERIFIED | Line 342-344: snapshot taken before device loop; line 366-367: `reloaded = {k: v.to(device) for k, v in original_state.items()}; model.load_state_dict(reloaded)` per device pass |
| 12 | `python main.py --mode attack_bench --dataset 2016.10a --ckpt_path ./checkpoint` runs the bench with no extra flags (D-14) | VERIFIED | `main.py:433`: `elif args.mode == 'attack_bench':` dispatcher; flags `--bench_n_samples` (default 512), `--bench_n_reps` (default 5), `--bench_batch_size` (default 128), `--bench_warmup` (default 3) all have defaults; ckpt loaded from `args.ckpt_path` |
| 13 | CLI flags `--bench_n_samples`, `--bench_n_reps`, `--bench_batch_size`, `--bench_warmup` merge into cfg via merge_args2cfg (D-14) | VERIFIED | `main.py:55-59`: all four argparse flags added with documented defaults; `cfg = merge_args2cfg(...)` is the project pattern that absorbs vars(args) |
| 14 | Dispatcher block lives between `adv_bench` and `multi_attack_eval` and forwards SNRs + test_idx for stratification (D-15) | VERIFIED | `main.py:422` (adv_bench) -> `main.py:433` (attack_bench) -> `main.py:465` (multi_attack_eval); ordering is correct. Forwards `snrs_test=snrs_test` (sliced from SNRs by test_idx) and `test_idx=test_idx` |
| 15 | Default ta_box='unit' to match D-05 paper hyperparameters | VERIFIED | `main.py:444`: `cfg.ta_box = 'unit'` set before bench call; engine reads ta_box at line 333 |
| 16 | Default cw_c=1.0, cw_steps=100, cw_lr=0.01 applied before create_attack reads cfg (D-05) | VERIFIED | `main.py:445-449`: `cfg.cw_c = 1.0`, `cfg.cw_steps = 100`, `cfg.cw_lr = 0.01`, `cfg.ead_max_iterations = 100`, `cfg.attack_eps = 0.03` all set before `run_attack_bench_5x2(...)` |
| 17 | Model checkpoint loaded via `torch.load(..., weights_only=True)` per project convention | VERIFIED | `main.py:440`: `ckpt = torch.load(resolve_ckpt_path(args.ckpt_path), map_location=cfg.device, weights_only=True)` |
| 18 | Standalone script `paper/scripts/plot_attack_bench_latency.py` reads attack_bench.csv and emits IEEE-styled PDF with grouped bars, log-y axis, std error bars (D-10) | VERIFIED | Script exists (5245 bytes); imports `apply_ieee_style`, `single_col_fig`, `DEFENSE_COLORS` from `ieee_style`; `set_yscale('log')` at line 120; `yerr=stds` at line 114; PDF output exists (13916 bytes, 1 page, valid PDF v1.4) |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `util/attack_bench.py` | Module exposing `run_attack_bench_5x2` + helpers | VERIFIED | 16752 bytes; defines `run_attack_bench_5x2`, `_collect_env`, `_stratified_indices`, `_iter_batches`, `_time_one_cell`, `_stamp_paper_defaults`; ATTACKS_5 constant; parses cleanly |
| `main.py` (attack_bench dispatcher) | argparse flags + dispatcher branch | VERIFIED | Lines 55-59: 4 `--bench_*` flags; lines 433-463: dispatcher branch between `adv_bench` and `multi_attack_eval`; loads ckpt with `weights_only=True`; pins paper hyperparameters |
| `inference/2016.10a_165/result/attack_bench.csv` | 10-row latency table | VERIFIED | 12 lines (1 env-comment + 1 header + 10 rows); 5 attacks × 2 devices verified via pandas; columns match D-08 schema. CAVEAT: smoke-budget values (n=64, reps=2) — see human_verification[1] |
| `inference/2016.10a_165/result/attack_bench_env.json` | Reproducibility metadata | VERIFIED | Non-empty JSON; contains `env` (torch_version, cuda_version, gpu_name, platform, python_version), `rows` (10 raw row dicts), `config` (n_samples, n_reps, batch_size, warmup, ta_box, attacks list) |
| `paper/scripts/plot_attack_bench_latency.py` | Standalone CSV→PDF plotter | VERIFIED | 5245 bytes; defines `_resolve_default_csv`, `_load_and_validate`, `_plot`, `main`; validates 10-row 5x2 shape; uses IEEE styling |
| `paper/latex/figures/attack_bench_latency.pdf` | Camera-ready bar chart | VERIFIED | 13916 bytes; `file` reports `PDF document, version 1.4, 1 pages` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `util/attack_bench.py` | `util/sigguard_eval.create_attack` | import + call inside device loop | WIRED | Line 61: `from util.sigguard_eval import create_attack, generate_adversarial`; line 377: `attack = create_attack(attack_name, wrapped_model, cfg)` |
| `util/attack_bench.py` | `util/adv_attack.Model01Wrapper` | wrap base model per device pass | WIRED | Line 60: import; line 370: `wrapped_model = Model01Wrapper(model).to(device).eval()` |
| `util/attack_bench.py` | `util/sigguard_eval.generate_adversarial` | execute attack on each timed batch | WIRED | Lines 218-224 (warmup) and 233-239 (timed): `_ = generate_adversarial(attack, batch_x, batch_y, wrapped_model=wrapped_model, ta_box=ta_box)` |
| `main.py` (attack_bench handler) | `util/attack_bench.run_attack_bench_5x2` | import + call | WIRED | Line 437: `from util.attack_bench import run_attack_bench_5x2`; line 455: invocation with all required args + snrs_test/test_idx kwargs |
| argparse `--bench_*` flags | `cfg` via `merge_args2cfg` | vars(args) → cfg fields | WIRED | Lines 55-59 register the 4 flags with documented defaults; project's standard `merge_args2cfg(cfg, vars(args))` is invoked at the top of `__main__` (existing pattern unchanged) |
| `paper/scripts/plot_attack_bench_latency.py` | `paper/scripts/ieee_style.py` | sibling sys.path import | WIRED | Lines 26-29: `sys.path.insert(0, os.path.dirname(...))`; `from ieee_style import apply_ieee_style, single_col_fig, IEEE_COL_WIDTH, DEFENSE_COLORS`; `apply_ieee_style()` called at line 97 |
| `paper/scripts/plot_attack_bench_latency.py` | `inference/2016.10a_*/result/attack_bench.csv` | pandas.read_csv with comment='#' | WIRED | Line 75: `df = pd.read_csv(csv_path, comment='#')`; default csv resolved via glob at line 56-70 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `attack_bench.csv` | `rows` list | Built per-(device, attack) cell from `_time_one_cell` returns; appended dicts containing real `mean_ms_per_sample`, `std_ms_per_sample`, `total_seconds` from actual `time.perf_counter()` measurements | Yes — CSV rows 3-12 contain non-zero, plausible measurements (e.g. `eaden,cpu,...,146.110225,5.033362`; `fgsm,cuda,...,0.084321,0.001421`); CPU/GPU ratio matches expectation (CPU ~5x slower for FGSM, much larger gap for EAD) | FLOWING |
| `attack_bench.pdf` | bar heights | `df['mean_ms_per_sample']` per (attack, device) read from validated CSV; std error bars from `df['std_ms_per_sample']` | Yes — log-scale axis renders all 10 bars; values span 0.084 (cuda fgsm) to 146.11 (cpu eaden), justifying log-y choice (D-10) | FLOWING |
| `attack_bench_env.json` | env metadata + rows | `_collect_env()` reads `torch.__version__`, `torch.version.cuda`, `torch.cuda.get_device_name(0)`, `platform.processor()` etc. | Yes — JSON contains real GPU name "NVIDIA GeForce RTX 5060 Ti", torch=2.9.0+cu130, cuda=13.0, all 10 row dicts populated | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `util/attack_bench.py` parses cleanly | `python -c "import ast; ast.parse(open('util/attack_bench.py').read())"` | exit 0 | PASS |
| `main.py` parses cleanly | `python -c "import ast; ast.parse(open('main.py').read())"` | exit 0 | PASS |
| `paper/scripts/plot_attack_bench_latency.py` parses cleanly | `python -c "import ast; ast.parse(...)"` | exit 0 | PASS |
| Required attack patterns present | `grep -c "torch.cuda.synchronize()" util/attack_bench.py` | 2 (>=2 required) | PASS |
| ATTACKS_5 list correct | `grep "ATTACKS_5 = " util/attack_bench.py` | `ATTACKS_5 = ['fgsm', 'pgd', 'cw', 'eadl1', 'eaden']` | PASS |
| CSV row count + shape | pandas: `len(df)==10`, attacks={cw,eaden,eadl1,fgsm,pgd}, devices={cpu,cuda} | all assertions pass | PASS |
| Dispatcher present and ordered | `grep -n "args.mode == 'attack_bench'" main.py` | line 433 (between adv_bench@422 and multi_attack_eval@465) | PASS |
| PDF is valid | `file paper/latex/figures/attack_bench_latency.pdf` | `PDF document, version 1.4, 1 pages` | PASS |
| End-to-end smoke run produces artifacts | (User executed before this verifier) | CSV+JSON+PDF exist on disk under inference/2016.10a_165/ and paper/latex/figures/ | PASS |
| Full-budget run (n=512, reps=5) completes and produces canonical numbers | (Not executed — would require 5+ min GPU wall time) | n/a | SKIP — routed to human_verification[1] |

### Requirements Coverage

This phase has no requirement IDs (added as a measurement phase outside the original requirement set, per CONTEXT.md). No coverage table needed.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `util/attack_bench.py` | 322 | `_ = test_idx  # silence linters; intentional.` | Info | Parameter accepted for forward compatibility but not yet consumed by stratifier (snrs_test already provides full (snr, label) info). Documented in code; no functional impact. |
| `inference/2016.10a_165/result/attack_bench.csv` | rows 3-12 | `n_samples=64, n_reps=2` instead of paper-budget 512/5 | Info (known) | Smoke-budget snapshot from initial verification; numbers will be regenerated at full budget before camera-ready submission. Documented in 07-03-SUMMARY.md "Pre-existing context drift" section. Wiring is verified complete; this is a re-execution task, not a code gap. |
| `paper/latex/figures/attack_bench_latency.pdf` | n/a | Reflects smoke-budget CSV | Info (known) | Same as above — PDF is regenerable from any CSV via `cd paper/scripts && python plot_attack_bench_latency.py`. |

No blocking anti-patterns. No TODO/FIXME/PLACEHOLDER markers. No empty handlers, no hardcoded empty arrays/objects flowing to user-visible output. The smoke-budget numbers issue is procedural (re-run with default args) not a code defect.

### Human Verification Required

#### 1. Visual inspection of `paper/latex/figures/attack_bench_latency.pdf`

**Test:** Open the PDF and visually confirm:
- 5 attack groups on the x-axis labelled FGSM, PGD, CW, EAD-L1, EAD-EN.
- 2 bars per group: gray (CPU), blue (GPU).
- y-axis is log-scaled and labelled `Latency (ms/sample)`.
- Std error bars visible on each bar.
- IEEE-style typography consistent with v1.0 paper figures.
- Legend ("CPU" / "GPU") visible and not occluded.

**Expected:** A clean, paper-ready figure suitable for `\includegraphics{figures/attack_bench_latency}` in `paper/latex/main.tex`.

**Why human:** Visual layout and styling cannot be verified programmatically. Programmatic checks confirmed the PDF is a 1-page valid PDF (13.9 KB) and the script invokes `apply_ieee_style()`, `single_col_fig()`, `set_yscale('log')`, and passes `yerr=stds` — but legend overlap, error-bar visibility, and font rendering need an eye.

#### 2. Full-budget regeneration before camera-ready submission

**Test:** Execute, on the GPU host:
```bash
source venv/bin/activate
python main.py --mode attack_bench --dataset 2016.10a --ckpt_path ./checkpoint
cd paper/scripts && python plot_attack_bench_latency.py
```

Then verify in the newest `inference/2016.10a_*/result/attack_bench.csv`:
- `n_samples=512` for all 10 rows.
- `n_reps=5` for all 10 rows.
- Numbers are stable (std typically <10% of mean) compared to the smoke-budget snapshot.

The PDF should auto-regenerate against the new CSV.

**Expected:** Canonical paper-budget latency table (5 attacks × 2 devices, N=512, R=5) with numbers reflecting the production hyperparameter set. PDF visualizes those numbers.

**Why human:** The current CSV/PDF (inference/2016.10a_165/) reflect a smoke-budget run (n=64, reps=2) — explicitly flagged in 07-03-SUMMARY.md "Pre-existing context drift" and reaffirmed by the orchestrator prompt. The wiring is complete and verified; this is a re-execution task that requires GPU wall time (~5+ min for CW/EAD on CPU) and cannot be performed inside the verifier.

### Gaps Summary

No blocking gaps found. All 18 must-haves verified, all 6 artifacts present and substantive, all 7 key links wired with imports and call sites, all 3 data-flow traces show real data flowing through the pipeline.

The only outstanding items are:
1. Visual inspection of the PDF (subjective, requires human eye).
2. Re-running the bench at full budget (N=512, R=5) before camera-ready submission — explicitly anticipated and documented as a future action in 07-03-SUMMARY.md, not a defect in this phase's deliverables.

The implementation correctly encodes all D-01..D-16 invariants from CONTEXT.md, including the CUDA-required halt, sync-bracketed timing, state_dict reload between device passes, R=5 reps with mean+std reporting, fixed batch_size=128, paper-default hyperparameter pinning, and the env-metadata JSON sidecar. The dispatcher branch is correctly positioned between `adv_bench` and `multi_attack_eval` and uses `weights_only=True` per project convention. The figure plotter validates the 5x2=10-row shape before plotting and uses the project's IEEE styling helpers.

---

*Verified: 2026-04-26T18:10:00Z*
*Verifier: Claude (gsd-verifier)*
