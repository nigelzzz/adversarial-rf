---
phase: 02-experimental-results
plan: 05
type: execute
wave: 2
depends_on: [02-04]
files_modified: []
autonomous: false
gap_closure: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05]

must_haves:
  truths:
    - "defense_compare.csv exists with rows for 9 defenses x 5 attacks x 10 SNR points plus weighted_avg"
    - "5 per-attack pivot CSVs exist (defense_compare_cw.csv, defense_compare_eadl1.csv, defense_compare_eaden.csv, defense_compare_fgsm.csv, defense_compare_pgd.csv)"
    - "18 confusion matrix .npy files exist (3 attacks x 3 SNRs x before/after)"
    - "budget_curves_detail.csv and budget_curves_agg.csv exist with Linf eps sweep and optimization c sweep data"
    - "calibration_params.json exists with per-SNR best parameters for all 5 classical filters"
  artifacts:
    - path: "inference/2016.10a_*/result/calibration_params.json"
      provides: "Per-SNR calibrated filter parameters"
      contains: "kalman"
    - path: "inference/2016.10a_*/result/defense_compare/defense_compare.csv"
      provides: "Full defense comparison matrix"
      contains: "ae_fft_topk"
    - path: "inference/2016.10a_*/result/defense_compare/defense_compare_cw.csv"
      provides: "CW attack pivot table"
      contains: "defense"
    - path: "inference/2016.10a_*/result/defense_compare/confmat/cw_snr0_before.npy"
      provides: "CW confusion matrix at SNR=0 before defense"
    - path: "inference/2016.10a_*/result/defense_compare/budget_curves/budget_curves_detail.csv"
      provides: "Perturbation budget curve detail data"
      contains: "param_value"
  key_links:
    - from: "calibration_params.json"
      to: "defense_compare.csv"
      via: "defense_compare loads calibrated params before evaluation"
      pattern: "calibration"
---

<objective>
Run all GPU experiments to produce the missing CSV and .npy artifacts for Phase 2.

Purpose: Gaps 1-3 from VERIFICATION.md — the evaluation infrastructure is complete but experiments have never been run. Gap 4 (calibration fix) is resolved by Plan 04. This plan runs the calibration sweep first, then the full defense comparison including confusion matrices and budget curves.

Output: calibration_params.json, defense_compare.csv, 5 per-attack pivot CSVs, 18 confusion matrix .npy files, budget curve CSVs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-experimental-results/02-CONTEXT.md
@.planning/phases/02-experimental-results/02-VERIFICATION.md
@.planning/phases/02-experimental-results/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run calibration sweep to produce calibration_params.json</name>
  <files></files>
  <read_first>
    - util/defense_calibrate.py (lines 237-346 run_calibration_sweep — understand what it does and what it produces)
    - main.py (the new --mode calibrate_defenses block added by Plan 04)
  </read_first>
  <action>
    Run the defense filter calibration sweep to produce calibration_params.json.

    This uses CW attacks with minmax normalization against each classical filter at each SNR point to find optimal filter parameters. Expected runtime: 30-60 minutes on GPU (5 filters x ~84 param combos x 10 SNRs, capped at 200 samples per SNR).

    **Command:**
    ```bash
    cd /home/nigel/opensource/adversarial-rf
    source venv/bin/activate
    python main.py --mode calibrate_defenses --dataset 2016.10a --ckpt_path ./checkpoint --max_per_cell 200
    ```

    **Expected output:**
    - File: `inference/2016.10a_*/result/calibration_params.json`
    - JSON with keys: kalman, wiener, savitzky_golay, gaussian, fir
    - Each key maps SNR (as string) to best parameter dict
    - Console: summary table showing best params per (filter, SNR)

    **If the run fails or takes too long:**
    - Check GPU memory: `nvidia-smi` — the AWN model + CW attack need ~2-4 GB
    - If OOM, reduce batch size or max_per_cell: `--max_per_cell 100`
    - If import errors, check Plan 04 was executed (util/defense_compare.py updated, main.py updated)

    **After the run completes:**
    - Verify the JSON file exists and contains all 5 filters
    - Verify each filter has entries for SNR points 0, 2, 4, ..., 18
    - Note the output directory path for use in Task 2
  </action>
  <verify>
    <automated>cd /home/nigel/opensource/adversarial-rf && python3 -c "
import json, glob
files = sorted(glob.glob('inference/*/result/calibration_params.json'))
assert files, 'No calibration_params.json found'
with open(files[-1]) as f:
    data = json.load(f)
filters = set(data.keys())
expected = {'kalman','wiener','savitzky_golay','gaussian','fir'}
assert expected.issubset(filters), f'Missing filters: {expected - filters}'
for filt in expected:
    snrs = set(data[filt].keys())
    assert len(snrs) >= 5, f'{filt} has only {len(snrs)} SNR entries'
print(f'PASS: calibration_params.json has {len(filters)} filters, file={files[-1]}')
"</automated>
  </verify>
  <acceptance_criteria>
    - glob.glob('inference/*/result/calibration_params.json') returns at least 1 file
    - JSON contains keys for all 5 classical filters
    - Each filter has per-SNR entries covering at least SNRs 0 through 18
  </acceptance_criteria>
  <done>
    calibration_params.json exists with per-SNR best parameters for all 5 classical filters (kalman, wiener, savitzky_golay, gaussian, fir). Each filter has calibrated parameters at each of the 10 evaluation SNR points.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full defense comparison (comparison table + confusion matrices + budget curves)</name>
  <files></files>
  <read_first>
    - The calibration_params.json file produced by Task 1 (to verify it exists and note its path)
    - util/defense_compare.py (lines 300-510 run_defense_compare — understand the evaluation loop)
    - main.py (lines 555-608 defense_compare mode — understand the full dispatch including confmat and budget)
  </read_first>
  <action>
    Run the full defense comparison evaluation to produce all missing CSV and .npy artifacts.

    This evaluates 9 defenses against 5 attacks at 10 SNR points, generates 18 confusion matrices, and runs perturbation budget curve sweeps. Expected runtime: 1-3 hours on GPU (CW and EAD attacks are slow at 200 samples/cell).

    **Step 1: Find the calibration_params.json path**

    ```bash
    ls -t inference/*/result/calibration_params.json | head -1
    ```

    Note this path. If auto-detection works (Plan 04 added it), you can omit --calibration_path.

    **Step 2: Run the full defense comparison**

    ```bash
    cd /home/nigel/opensource/adversarial-rf
    source venv/bin/activate
    python main.py --mode defense_compare --dataset 2016.10a --ckpt_path ./checkpoint \
      --detector_ckpt ./checkpoint/detector_ae.pth \
      --max_per_cell 200
    ```

    If auto-detection does not find calibration_params.json, add explicitly:
    ```bash
    python main.py --mode defense_compare --dataset 2016.10a --ckpt_path ./checkpoint \
      --detector_ckpt ./checkpoint/detector_ae.pth \
      --max_per_cell 200 \
      --calibration_path inference/2016.10a_XXX/result/calibration_params.json
    ```

    **Expected output files (all in inference/2016.10a_*/result/defense_compare/):**

    Comparison table (Gap 1 / EVAL-01, EVAL-02, EVAL-05):
    - defense_compare.csv — full matrix (attack, snr, defense, accuracy, n_samples)
    - defense_compare_cw.csv — CW pivot (rows=defense, cols=SNR)
    - defense_compare_eadl1.csv — EAD-L1 pivot
    - defense_compare_eaden.csv — EAD-EN pivot
    - defense_compare_fgsm.csv — FGSM pivot
    - defense_compare_pgd.csv — PGD pivot

    Confusion matrices (Gap 2 / EVAL-03):
    - confmat/cw_snr0_before.npy, cw_snr0_after.npy
    - confmat/cw_snr10_before.npy, cw_snr10_after.npy
    - confmat/cw_snr18_before.npy, cw_snr18_after.npy
    - confmat/eadl1_snr0_before.npy, eadl1_snr0_after.npy
    - confmat/eadl1_snr10_before.npy, eadl1_snr10_after.npy
    - confmat/eadl1_snr18_before.npy, eadl1_snr18_after.npy
    - confmat/eaden_snr0_before.npy, eaden_snr0_after.npy
    - confmat/eaden_snr10_before.npy, eaden_snr10_after.npy
    - confmat/eaden_snr18_before.npy, eaden_snr18_after.npy
    - confmat/confmat_summary.csv

    Budget curves (Gap 3 / EVAL-04):
    - budget_curves/budget_curves_detail.csv
    - budget_curves/budget_curves_agg.csv
    - budget_curves/budget_fgsm.csv, budget_pgd.csv, budget_cw.csv, budget_eadl1.csv, budget_eaden.csv

    **If the run crashes partway:**
    - CW/EAD attacks can OOM on large batches. Re-run with smaller batch size by adding `--eval_limit_per_cell 100`
    - If only budget curves fail, re-run with `--skip_confmat` to skip the already-completed confmat stage
    - If only confmat fails, re-run with `--skip_budget` to skip budget curves

    **After the run completes:**
    - Verify all expected files exist
    - Spot-check defense_compare_cw.csv: ae_fft_topk row should show higher accuracy than classical filter rows
    - Spot-check budget_curves_agg.csv: accuracy should decrease as epsilon/c increases
  </action>
  <verify>
    <automated>cd /home/nigel/opensource/adversarial-rf && python3 -c "
import glob, os
dc_csvs = sorted(glob.glob('inference/*/result/defense_compare/defense_compare.csv'))
assert dc_csvs, 'No defense_compare.csv found'
import pandas as pd
df = pd.read_csv(dc_csvs[-1])
attacks = set(df['attack'].unique())
expected_attacks = {'cw','eadl1','eaden','fgsm','pgd'}
assert expected_attacks.issubset(attacks), f'Missing attacks: {expected_attacks - attacks}'
defenses = set(df['defense'].unique())
assert len(defenses) >= 9, f'Only {len(defenses)} defenses, expected 9'
base_dir = os.path.dirname(dc_csvs[-1])
for atk in expected_attacks:
    pivot_path = os.path.join(base_dir, f'defense_compare_{atk}.csv')
    assert os.path.isfile(pivot_path), f'Missing pivot: {pivot_path}'
confmat_dir = os.path.join(base_dir, 'confmat')
npy_files = glob.glob(os.path.join(confmat_dir, '*.npy'))
assert len(npy_files) >= 18, f'Only {len(npy_files)} .npy files, expected 18'
summary = os.path.join(confmat_dir, 'confmat_summary.csv')
assert os.path.isfile(summary), 'Missing confmat_summary.csv'
budget_dir = os.path.join(base_dir, 'budget_curves')
detail = os.path.join(budget_dir, 'budget_curves_detail.csv')
agg = os.path.join(budget_dir, 'budget_curves_agg.csv')
assert os.path.isfile(detail), 'Missing budget_curves_detail.csv'
assert os.path.isfile(agg), 'Missing budget_curves_agg.csv'
print(f'ALL CHECKS PASSED: {len(df)} rows in defense_compare.csv, {len(npy_files)} confmat .npy files')
"</automated>
  </verify>
  <acceptance_criteria>
    - defense_compare.csv has rows covering 5 attacks x 9 defenses x 10 SNR points (450+ data rows)
    - 5 per-attack pivot CSVs exist (defense_compare_cw.csv etc.)
    - 18 confusion matrix .npy files in confmat/ directory
    - confmat_summary.csv exists
    - budget_curves_detail.csv and budget_curves_agg.csv exist
    - 5 per-attack budget curve pivot CSVs exist
  </acceptance_criteria>
  <done>
    All experimental data artifacts exist: full defense comparison matrix CSV with 9 defenses x 5 attacks x 10 SNR points, 5 per-attack pivot tables, 18 confusion matrix .npy files with summary, and perturbation budget curve CSVs with detail and aggregate views. All evaluations used calibrated filter parameters from calibration_params.json.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify unified pipeline outperforms classical filters</name>
  <files></files>
  <action>
    Human reviews the experimental results to confirm Success Criterion 4 from the ROADMAP: the unified pipeline (ae_fft_topk) outperforms every classical filter baseline on at least the two strongest attacks (CW, EAD).
  </action>
  <verify>Human confirms by typing "approved" or describing issues</verify>
  <done>Human has confirmed the unified pipeline outperforms classical filters on CW and EAD attacks, or has described issues to address.</done>
  <what-built>
    Full defense comparison experiments have been run producing:
    1. Defense comparison table (9 defenses x 5 attacks x 10 SNRs) with calibrated filter parameters
    2. 18 confusion matrices (3 attacks x 3 SNRs x before/after defense)
    3. Perturbation budget curves (8 Linf eps points + 4 optimization c values)
  </what-built>
  <how-to-verify>
    1. Open the most recent defense_compare_cw.csv pivot table:
       `cat inference/2016.10a_*/result/defense_compare/defense_compare_cw.csv | column -t -s,`
    2. Verify ae_fft_topk row has higher accuracy than classical filter rows (kalman, wiener, savitzky_golay, gaussian, fir) at most SNR points — this is Success Criterion 4 from the ROADMAP
    3. Open budget_curves_agg.csv and verify accuracy drops as epsilon/c increases (attacks are working)
    4. Check confmat_summary.csv has 18 rows covering all (attack, SNR, condition) combos
    5. Verify calibration_params.json was loaded (check the log output for "Loaded calibration params from" message)
  </how-to-verify>
  <resume-signal>Type "approved" if the unified pipeline outperforms classical filters on CW/EAD attacks. Otherwise describe which results look wrong.</resume-signal>
</task>

</tasks>

<verification>
1. `find inference/ -name "defense_compare.csv" | head -1` returns a path
2. `find inference/ -name "*.npy" -path "*/confmat/*" | wc -l` returns >= 18
3. `find inference/ -name "budget_curves_detail.csv" | head -1` returns a path
4. `find inference/ -name "calibration_params.json" | head -1` returns a path
5. In defense_compare_cw.csv, ae_fft_topk has higher accuracy than all 5 classical filters at weighted_avg
</verification>

<success_criteria>
- All 4 verification gaps from 02-VERIFICATION.md are closed
- calibration_params.json exists with per-SNR best params for 5 filters (Gap 4)
- defense_compare.csv + 5 pivot CSVs exist with correct dimensions (Gap 1)
- 18 confusion matrix .npy files exist (Gap 2)
- Budget curve CSVs exist with eps sweep and c sweep data (Gap 3)
- Human has confirmed ae_fft_topk outperforms classical filters on CW/EAD (Success Criterion 4)
</success_criteria>

<output>
After completion, create `.planning/phases/02-experimental-results/02-05-SUMMARY.md`
</output>
