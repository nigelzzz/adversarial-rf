---
phase: 03-paper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/latex/main.tex
  - paper/latex/refs.bib
  - paper/scripts/ieee_style.py
  - paper/scripts/generate_figures.py
  - paper/latex/figures/
autonomous: true
requirements: [PAPER-01, PAPER-08, PAPER-09]

must_haves:
  truths:
    - "main.tex compiles without errors using latexmk -pdf"
    - "All 10+ paper figures exist as PDF files in paper/latex/figures/"
    - "Figures use IEEE-standard dimensions (3.487in single-col, 7.16in double-col) with 8pt serif fonts"
    - "refs.bib contains 35+ references spanning AMC, adversarial ML, classical filtering, and defense methods"
    - "Compiled PDF shows IEEEtran journal double-column layout with section stubs"
  artifacts:
    - path: "paper/latex/main.tex"
      provides: "IEEEtran journal master document with preamble, author block, abstract placeholder, section inputs, bibliography"
      contains: "\\documentclass[journal]{IEEEtran}"
    - path: "paper/latex/refs.bib"
      provides: "Complete BibTeX bibliography with 35+ references"
      min_lines: 300
    - path: "paper/scripts/ieee_style.py"
      provides: "Shared matplotlib IEEE styling configuration"
      exports: ["IEEE_STYLE", "IEEE_COL_WIDTH", "IEEE_DBL_WIDTH", "apply_ieee_style"]
    - path: "paper/scripts/generate_figures.py"
      provides: "Single script that reads all Phase 2 CSVs/NPYs and produces all paper figures as PDFs"
      min_lines: 200
    - path: "paper/latex/figures/defense_compare_overview.pdf"
      provides: "Grouped bar chart of all defenses across all attacks (weighted avg)"
    - path: "paper/latex/figures/acc_vs_snr_cw.pdf"
      provides: "Accuracy vs SNR line plot for CW attack"
    - path: "paper/latex/figures/acc_vs_snr_eadl1.pdf"
      provides: "Accuracy vs SNR line plot for EAD-L1 attack"
    - path: "paper/latex/figures/confmat_cw_snr18.pdf"
      provides: "Side-by-side confusion matrix before/after defense for CW at SNR=18"
    - path: "paper/latex/figures/confmat_eadl1_snr18.pdf"
      provides: "Side-by-side confusion matrix before/after defense for EAD-L1 at SNR=18"
    - path: "paper/latex/figures/budget_fgsm.pdf"
      provides: "Perturbation budget curve for FGSM"
    - path: "paper/latex/figures/budget_cw.pdf"
      provides: "Perturbation budget curve for CW"
    - path: "paper/latex/figures/freq_spectra_cw.pdf"
      provides: "Frequency-domain spectra: clean vs CW-attacked vs Adaptive-K-recovered"
  key_links:
    - from: "paper/scripts/generate_figures.py"
      to: "inference/2016.10a_165/result/defense_compare/"
      via: "pandas.read_csv and numpy.load"
      pattern: "read_csv|np\\.load"
    - from: "paper/scripts/generate_figures.py"
      to: "paper/scripts/ieee_style.py"
      via: "import"
      pattern: "from ieee_style import"
    - from: "paper/latex/main.tex"
      to: "paper/latex/figures/"
      via: "\\includegraphics"
      pattern: "includegraphics"
    - from: "paper/latex/main.tex"
      to: "paper/latex/refs.bib"
      via: "\\bibliography{refs}"
      pattern: "bibliography.refs."
---

<objective>
Create the complete LaTeX document infrastructure, IEEE-quality figure generation pipeline, and expanded bibliography for the IEEE TCCN paper.

Purpose: Establish the compilation pipeline and all visual assets so that subsequent plans can focus purely on writing content into section files. Every figure referenced in the paper must already exist as a PDF before content writing begins.

Output: Compilable main.tex with section stubs, 35+ reference bibliography, ieee_style.py module, generate_figures.py that produces all 10+ paper figures from Phase 2 data, and all figure PDFs in paper/latex/figures/.
</objective>

<execution_context>
@/home/nigel/.claude/get-shit-done/workflows/execute-plan.md
@/home/nigel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-paper/03-CONTEXT.md
@.planning/phases/03-paper/03-RESEARCH.md

Source data for figures:
@inference/2016.10a_165/result/defense_compare/defense_compare.csv
@inference/2016.10a_165/result/defense_compare/confmat/confmat_summary.csv
@inference/2016.10a_165/result/defense_compare/budget_curves/budget_fgsm.csv
@inference/2016.10a_165/result/defense_compare/budget_curves/budget_cw.csv

Existing USENIX draft (for bibliography seeds):
@paper/latex/spectral_gated_defense_usenix.bib
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main.tex, section stubs, ieee_style.py, refs.bib</name>
  <files>
    paper/latex/main.tex,
    paper/latex/sections/introduction.tex,
    paper/latex/sections/related_work.tex,
    paper/latex/sections/system_model.tex,
    paper/latex/sections/proposed_method.tex,
    paper/latex/sections/experimental_setup.tex,
    paper/latex/sections/results.tex,
    paper/latex/sections/conclusion.tex,
    paper/scripts/ieee_style.py,
    paper/latex/refs.bib
  </files>
  <action>
    **1. Create directory structure:**
    ```
    mkdir -p paper/latex/sections paper/latex/figures paper/scripts
    ```

    **2. Create `paper/latex/main.tex`** — IEEEtran journal master document:
    - `\documentclass[journal]{IEEEtran}`
    - Packages: amsmath, amssymb, booktabs, graphicx, algorithm, algpseudocode, subcaption, multirow, xcolor, cite, url, hyperref (load hyperref LAST)
    - `\graphicspath{{figures/}}`
    - Title: "Adaptive Spectral Defense for Real-Time Recovery of Deep-Learning-Based Modulation Classifiers Under Adversarial Attack"
    - Author block with `\IEEEmembership{}` placeholder
    - `\markboth{IEEE Trans. on Cognitive Commun. and Netw.}{Author \MakeLowercase{\textit{et al.}}: Adaptive Spectral Defense}`
    - Abstract placeholder (3-4 sentence stub referencing contributions)
    - `\begin{IEEEkeywords}adversarial machine learning, automatic modulation classification, input transformation defense, spectral filtering, RF security\end{IEEEkeywords}`
    - Seven `\input{sections/...}` for each section file
    - `\bibliographystyle{IEEEtran}` and `\bibliography{refs}`

    **3. Create 7 section stub files** in `paper/latex/sections/`:
    Each stub should contain the section header and a `% TODO: content` comment so the paper compiles:
    - `introduction.tex`: `\section{Introduction}` + `% Content in Plan 02`
    - `related_work.tex`: `\section{Related Work}` with subsections A/B/C stubs
    - `system_model.tex`: `\section{System Model and Threat Model}` with subsections A/B/C/D stubs
    - `proposed_method.tex`: `\section{Proposed Defense Methods}` with subsections A-F stubs
    - `experimental_setup.tex`: `\section{Experimental Setup}` with subsections A-D stubs
    - `results.tex`: `\section{Results and Analysis}` with subsections A-F stubs
    - `conclusion.tex`: `\section{Conclusion}` + stub

    **4. Create `paper/scripts/ieee_style.py`** — Shared matplotlib IEEE configuration:
    - Constants: `IEEE_COL_WIDTH = 3.487`, `IEEE_DBL_WIDTH = 7.16`, `GOLDEN_RATIO = 1.618`
    - `IEEE_STYLE` dict with: `font.family: serif`, `font.serif: ['Times', 'Times New Roman', 'Computer Modern Roman']`, `font.size: 8`, `text.usetex: True`, `axes.labelsize: 8`, `axes.titlesize: 9`, `xtick.labelsize: 7`, `ytick.labelsize: 7`, `legend.fontsize: 7`, `figure.dpi: 300`, `savefig.dpi: 300`, `savefig.format: pdf`, `savefig.bbox: tight`, `savefig.pad_inches: 0.02`, `lines.linewidth: 1.0`, `lines.markersize: 3`, `axes.linewidth: 0.5`, `grid.linewidth: 0.3`
    - Function `apply_ieee_style()` that calls `plt.rcParams.update(IEEE_STYLE)`
    - Function `single_col_fig()` returning `plt.subplots(figsize=(IEEE_COL_WIDTH, IEEE_COL_WIDTH / GOLDEN_RATIO))`
    - Function `double_col_fig(height_ratio=0.5)` returning `plt.subplots(figsize=(IEEE_DBL_WIDTH, IEEE_DBL_WIDTH * height_ratio))`
    - Color palette dict `DEFENSE_COLORS` mapping defense names to distinct colors: adaptive_k (blue), spectral_gated (green), no_defense (gray), kalman (orange), wiener (red), savitzky_golay (purple), gaussian (brown), fir (pink), rand_smooth (olive)
    - `DEFENSE_LABELS` dict mapping code names to paper-friendly names: adaptive_k -> "Adaptive-K", spectral_gated -> "Spectral-Gated", no_defense -> "No Defense", kalman -> "Kalman", wiener -> "Wiener", savitzky_golay -> "Savitzky-Golay", gaussian -> "Gaussian", fir -> "FIR", rand_smooth -> "Rand. Smooth."

    **5. Create `paper/latex/refs.bib`** — Expanded bibliography with 35+ entries:
    Start from the 10 references in the USENIX draft `.bib` and expand to cover these categories:
    - **AMC classifiers (5-6 refs):** AWN (li2023awn), O'Shea CNN (otoole2016rml), CLDNN, ResNet for AMC, LSTM-based AMC, RML2016 dataset paper
    - **Adversarial attacks on AMC (5-6 refs):** Silvaco survey (silvaco2023adversarial_rf), Lin threats (lin2020tactics), Flowers SigGuard (flowers2019sigguard), Zhang stealthy attacks (2024), Homomorphic filtering defense (2024), frequency-domain enhancement defense (2025)
    - **General adversarial ML (5-6 refs):** Carlini CW (carlini2017towards), Chen EAD (chen2020ead), Goodfellow FGSM (goodfellow2015fgsm), Madry PGD (madry2018pgd), Szegedy intriguing properties
    - **Defense methods (5-6 refs):** Guo input transformations (ICLR 2018), Cohen randomized smoothing (ICML 2019), Dziugaite JPG compression, adversarial training (Madry 2018), feature squeezing (Xu 2018)
    - **Classical filtering (4-5 refs):** Haykin adaptive filter theory, Savitzky-Golay original (1964), Wiener filter theory, Kalman filter, digital filter design textbook
    - **Communications/RF fundamentals (4-5 refs):** Proakis digital communications (proakis2008digital), Kim torchattacks (kim2020torchattacks), RML dataset papers (O'Shea 2016, 2018), IEEE spectrum sensing surveys
    - **Deep learning fundamentals (2-3 refs):** Hu squeeze-excitation networks, batch normalization, Adam optimizer
    - Each entry must have complete fields: author, title, journal/booktitle, year, pages/volume where available
    - Use consistent BibTeX key format: `authorYYYYkeyword` (e.g., `li2023awn`, `carlini2017towards`)
  </action>
  <verify>
    <automated>cd /home/nigel/opensource/adversarial-rf/paper/latex && latexmk -pdf -interaction=nonstopmode main.tex 2>&1 | tail -5</automated>
  </verify>
  <done>main.tex compiles to PDF with IEEEtran journal layout showing all section headings; refs.bib has 35+ entries; ieee_style.py exists with IEEE_STYLE dict and helper functions; 7 section stub files exist</done>
</task>

<task type="auto">
  <name>Task 2: Create generate_figures.py and produce all paper figure PDFs</name>
  <files>
    paper/scripts/generate_figures.py,
    paper/latex/figures/defense_compare_overview.pdf,
    paper/latex/figures/acc_vs_snr_cw.pdf,
    paper/latex/figures/acc_vs_snr_eadl1.pdf,
    paper/latex/figures/acc_vs_snr_fgsm.pdf,
    paper/latex/figures/acc_vs_snr_pgd.pdf,
    paper/latex/figures/confmat_cw_snr18.pdf,
    paper/latex/figures/confmat_eadl1_snr18.pdf,
    paper/latex/figures/confmat_eaden_snr18.pdf,
    paper/latex/figures/budget_fgsm.pdf,
    paper/latex/figures/budget_cw.pdf,
    paper/latex/figures/freq_spectra_cw.pdf
  </files>
  <action>
    Create `paper/scripts/generate_figures.py` — a single script that reads all Phase 2 experimental data and produces every figure needed for the paper as PDF files in `paper/latex/figures/`.

    **Script structure:**
    ```python
    #!/usr/bin/env python3
    """Generate all figures for IEEE TCCN paper from Phase 2 experimental data."""
    import sys, os
    sys.path.insert(0, os.path.dirname(__file__))
    from ieee_style import *
    ```

    **Data paths** (hardcoded relative to repo root, script should `os.chdir` or use absolute paths):
    - `inference/2016.10a_165/result/defense_compare/defense_compare.csv` — main results (495 rows: attack, snr, defense, accuracy, n_samples)
    - `inference/2016.10a_165/result/defense_compare/confmat/{attack}_snr{snr}_{before|after}.npy` — 11x11 int confusion matrices
    - `inference/2016.10a_165/result/defense_compare/budget_curves/budget_{attack}.csv` — columns: param_value, no_defense, adaptive_k, ..., rand_smooth

    **Figures to generate (10 total):**

    1. **`defense_compare_overview.pdf`** (double-column grouped bar chart):
       - Read defense_compare.csv, filter to weighted_avg rows (snr column will have "weighted_avg" or similar — check the CSV; the per-attack pivot CSVs like defense_compare_cw.csv have a `weighted_avg` column)
       - Actually: use the per-attack pivot CSVs (defense_compare_cw.csv etc.) which have defense as rows and SNR + weighted_avg as columns
       - Plot grouped bars: X-axis = defenses (9), groups = attacks (5: CW, EAD-L1, EAD-EN, FGSM, PGD)
       - Y-axis = accuracy (%), range 30-85%
       - Use DEFENSE_COLORS and DEFENSE_LABELS from ieee_style
       - Add horizontal dashed line at no_defense weighted_avg for each attack
       - Exclude rand_smooth from main figure (too low, distorts scale) OR include with a break in y-axis
       - Actually: include all 9 defenses but use a reasonable y-axis range; rand_smooth at ~40% will naturally separate
       - Sort defenses by mean accuracy: adaptive_k, spectral_gated, no_defense, kalman, wiener, gaussian, fir, savitzky_golay, rand_smooth

    2. **`acc_vs_snr_cw.pdf`** (single-column line plot):
       - From defense_compare.csv, filter attack=='cw'
       - Pivot: index=snr, columns=defense, values=accuracy
       - Plot all 9 defenses as lines with markers, using DEFENSE_COLORS
       - X-axis: SNR (dB) from 0 to 18 in even steps
       - Y-axis: Accuracy (%) range ~55-85%
       - Legend: use DEFENSE_LABELS, place in lower-right or best location
       - Grid on y-axis only (light)

    3. **`acc_vs_snr_eadl1.pdf`** (single-column, same layout as CW)

    4. **`acc_vs_snr_fgsm.pdf`** (single-column, same layout)

    5. **`acc_vs_snr_pgd.pdf`** (single-column, same layout)

    6. **`confmat_cw_snr18.pdf`** (double-column, side-by-side heatmaps):
       - Load confmat/cw_snr18_before.npy and cw_snr18_after.npy (11x11 int arrays)
       - Normalize each to row percentages: `cm_pct = cm / cm.sum(axis=1, keepdims=True) * 100`
       - Use `fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(IEEE_DBL_WIDTH, IEEE_DBL_WIDTH * 0.45))`
       - Left: "Before Defense" heatmap, Right: "After Defense (Adaptive-K)" heatmap
       - Modulation labels (11 classes): `['QAM16', 'QAM64', '8PSK', 'WBFM', 'BPSK', 'CPFSK', 'AM-DSB', 'GFSK', 'PAM4', 'QPSK', 'AM-SSB']`
       - Use seaborn.heatmap with annot=True, fmt='.0f', cmap='Blues'
       - Rotate x-labels 45 degrees, y-labels horizontal, fontsize=5-6
       - Shared colorbar or separate colorbars

    7. **`confmat_eadl1_snr18.pdf`** (double-column, same layout as CW)

    8. **`confmat_eaden_snr18.pdf`** (double-column, same layout as CW)

    9. **`budget_fgsm.pdf`** (single-column line plot):
       - Load budget_curves/budget_fgsm.csv
       - X-axis: param_value (epsilon values), use log scale if values span orders of magnitude — for FGSM the values are 0.01, 0.03, 0.05, 0.1 so linear is fine
       - Y-axis: Accuracy (%)
       - Plot lines for key defenses: no_defense, adaptive_k, spectral_gated, kalman, wiener, fir (skip rand_smooth to reduce clutter, or include as dashed)
       - Markers, colors from ieee_style

    10. **`budget_cw.pdf`** (single-column line plot):
        - Load budget_curves/budget_cw.csv
        - X-axis: param_value (CW c values: 0.01, 0.1, 1.0, 10.0) — use log scale
        - Same defense line styling as budget_fgsm

    11. **`freq_spectra_cw.pdf`** (double-column, 3-panel frequency-domain spectra):
        - This figure shows FFT magnitude spectra for clean, CW-attacked, and Adaptive-K-recovered signals
        - Load a representative sample from the RML2016.10a test set (e.g., QPSK at SNR=18)
        - Generate CW adversarial example using torchattacks (same config as defense_compare: minmax, c=1.0)
        - Apply adaptive_k defense to the adversarial signal
        - Compute FFT magnitude (dB) for each of the 3 signals (use I channel)
        - Plot 3 subplots: (a) Clean, (b) CW-attacked, (c) After Adaptive-K recovery
        - Y-axis: |X(f)| in dB, X-axis: frequency bin index (0 to 63 for 128-pt FFT)
        - Use consistent y-axis range across all 3 panels
        - This requires importing the AWN model and torchattacks — add a try/except block so the script still works if model loading fails (skip this figure with a warning)

    **Script execution pattern:**
    - Script determines repo root via `os.path.dirname(os.path.abspath(__file__))` going up 2 levels
    - Each figure generated in its own function: `plot_defense_overview()`, `plot_acc_vs_snr(attack)`, `plot_confmat(attack, snr)`, `plot_budget(attack)`
    - Main block calls all functions, prints count of generated figures
    - Use `plt.close('all')` after each figure to prevent memory leaks
    - Handle missing .npy files gracefully (print warning, skip)

    **Run the script after creating it:**
    ```bash
    cd /home/nigel/opensource/adversarial-rf
    source venv/bin/activate
    python paper/scripts/generate_figures.py
    ```

    Verify all PDFs exist in paper/latex/figures/.
  </action>
  <verify>
    <automated>cd /home/nigel/opensource/adversarial-rf && source venv/bin/activate && python paper/scripts/generate_figures.py && ls paper/latex/figures/*.pdf | wc -l</automated>
  </verify>
  <done>generate_figures.py runs without errors; at least 10 PDF figure files exist in paper/latex/figures/; each figure uses IEEE dimensions (3.487in or 7.16in width) with serif fonts at 8pt; confusion matrix heatmaps show 11x11 grids with modulation labels; line plots show all relevant defenses with consistent colors</done>
</task>

</tasks>

<verification>
1. `cd paper/latex && latexmk -pdf main.tex` completes without errors
2. `ls paper/latex/figures/*.pdf | wc -l` shows >= 10 figure files
3. `grep -c '@' paper/latex/refs.bib` shows >= 35 BibTeX entries
4. `python -c "import paper.scripts.ieee_style; print(len(paper.scripts.ieee_style.IEEE_STYLE))"` shows style dict is populated
5. Compiled PDF shows double-column IEEEtran journal layout with section headers
</verification>

<success_criteria>
- main.tex compiles to a valid PDF with IEEEtran journal formatting
- All 10+ figure PDFs exist in paper/latex/figures/ at IEEE-standard dimensions
- refs.bib contains 35+ complete BibTeX entries across all required categories
- ieee_style.py provides reusable IEEE matplotlib configuration
- generate_figures.py can be re-run to regenerate all figures from Phase 2 data
</success_criteria>

<output>
After completion, create `.planning/phases/03-paper/03-01-SUMMARY.md`
</output>
